// Easy
// https://leetcode.com/problems/find-the-difference/

// Given two strings s and t which consist of only lowercase letters.
// String t is generated by random shuffling string s and then add one more letter at a random position.
// Find the letter that was added in t.

// Example:

// Input:
// s = "abcd"
// t = "abcde"

// Output:
// e

// Explanation:
// 'e' is the letter that was added.


// Runtime: 80 ms, faster than 78.50% of JavaScript online submissions for Find the Difference.
// Memory Usage: 38.1 MB, less than 60.29% of JavaScript online submissions for Find the Difference.
function findDiff( s1, s2 ) {
    let map = {};
    for( const char of s1 ) {
        map[char] = map[char]+1 || 1;
    };

    for( const char of s2 ) {
        if( !map[char] ) return char;
        else map[char]--;
    };
};

const str1 = 'abcd', str2 = 'abcde';

console.log( findDiff( str1, str2 ) );